# .env.example - Environment Variables Configuration

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# PostgreSQL connection string (choose one)
POSTGRES_URL=postgresql://user:password@localhost:5432/database
# OR
DATABASE_URL=postgresql://user:password@localhost:5432/database
# OR (Vercel Postgres)
VERCEL_POSTGRES_URL=postgresql://user:password@vercel.postgres.com/database

# ============================================================================
# EMBEDDING CONFIGURATION
# ============================================================================

# Local Embedding Service URL (deployed on Coolify)
# For local development: http://localhost:3001
# For production: https://embeddings.yourdomain.com
EMBEDDING_SERVICE_URL=http://localhost:3001

# Use local embeddings (FREE) vs OpenAI (PAID)
# Set to "false" to use OpenAI embeddings (costs money)
# Default: "true" (use free local embeddings)
USE_LOCAL_EMBEDDINGS=true

# OpenAI API Key (only needed if USE_LOCAL_EMBEDDINGS=false OR for GPT fallback)
OPENAI_API_KEY=sk-...

# OpenAI Embedding Model (only used if USE_LOCAL_EMBEDDINGS=false)
# Options: text-embedding-3-small, text-embedding-3-large, text-embedding-ada-002
# Default: text-embedding-3-large
EMBEDDING_MODEL=text-embedding-3-large

# GPT Fallback Model (used when similarity score is below threshold)
# Options: gpt-4o-mini, gpt-4o, gpt-3.5-turbo
# Default: gpt-4o-mini
FALLBACK_MODEL=gpt-4o-mini

# ============================================================================
# DEPLOYMENT CONFIGURATION
# ============================================================================

# Node environment
NODE_ENV=production

# Read-only filesystem flag (set to "1" for serverless/Vercel)
READ_ONLY_FS=0

# ============================================================================
# COST OPTIMIZATION NOTES
# ============================================================================

# RECOMMENDED SETUP (Zero Cost for Embeddings):
# USE_LOCAL_EMBEDDINGS=true
# EMBEDDING_SERVICE_URL=https://embeddings.yourdomain.com
# OPENAI_API_KEY=sk-... (only for GPT fallback when needed)

# OPENAI SETUP (Costs Money):
# USE_LOCAL_EMBEDDINGS=false
# OPENAI_API_KEY=sk-...
# EMBEDDING_MODEL=text-embedding-3-large

# COST COMPARISON:
# Local embeddings: $0 per 100,000 queries
# OpenAI embeddings: ~$13 per 100,000 queries (text-embedding-3-large)